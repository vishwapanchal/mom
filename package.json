{
  "name": "mother",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=vishwawishesmothersday"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
